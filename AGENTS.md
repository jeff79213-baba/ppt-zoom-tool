# 簡報縮放導覽工具（暫名）— 專案規劃書

版本：v1.0
日期：2026-08-16

---

## 一、專案簡介

一套給「上台簡報」情境使用的簡報播放/導覽工具。傳統 PowerPoint 播放模式只能整頁切換，這套工具讓講者可以在簡報過程中，即時把畫面局部放大、平移、手繪標註重點，操作方式類似 AutoCAD 的框選縮放，加上 PDF 閱讀器的手掌平移工具與螢幕畫筆工具。

**目標使用者**：需要上台簡報、教學、業務展示的人，簡報內容常有需要「放大講解」的細節（圖表、表格、程式碼、設計稿等）。

**核心價值**：不用切換軟體、不用預先做好放大版投影片，臨場就能自由縮放、平移、標註重點；且真正跨平台（桌面/網頁/手機），任何裝置都能用同一套邏輯操作。

---

## 二、市場定位與差異化

目前市面上沒有一款工具同時滿足「跨平台」＋「上傳任意簡報檔案」＋「縮放與手繪整合在同一套操作邏輯」。

| | ZoomIt | Epic Pen | PowerPoint 內建縮放 | **本專案** |
|---|---|---|---|---|
| 支援平台 | 僅 Windows | Windows / Mac | 綁死 PowerPoint | **桌面 / 網頁 / 手機（跨平台）** |
| 縮放方式 | 快捷鍵/滾輪縮放整個螢幕 | 無縮放功能 | +/-鍵、滑鼠拖曳框選 | **統一工具：中心點拖曳＋斜角框選** |
| 手繪標註 | 有，多色畫筆 | 有，介面直覺 | 有，但與縮放分開操作 | **有，痕跡保留可跨頁、有 Undo 保護** |
| 簡報檔案整合 | 無，僅操作整個螢幕 | 無，僅螢幕標註 | 僅限 PPT 檔案本身 | **PPT/PDF/圖片皆可上傳播放** |
| 行動裝置支援 | 不支援 | 不支援 | 受限於官方 App | **原生支援，觸控手勢與桌面邏輯一致** |

**差異化重點**：
1. 統一的檔案上傳流程，PPT / PDF / 圖片都能直接播放
2. 平移、縮放、手繪三個工具用同一套「點工具列切換」邏輯，不需背多組快捷鍵
3. 真正跨平台，現有工具幾乎都卡在 Windows-only
4. 手繪痕跡可跨頁保留，不像現有工具多是「離開即清空」的即時標註

---

## 三、核心功能規格

### 1. 簡報上傳與播放

| 項目 | 說明 |
|---|---|
| 上傳格式 | PPTX、PDF、圖片，統一轉成靜態頁面（不保留原始動畫效果） |
| 超連結支援 | 轉檔時解析原始檔案中超連結的座標與網址，於靜態頁面上疊加一層透明可點擊區域，播放時連結仍可點擊開啟網頁 |
| 翻頁（桌面） | 鍵盤「↑ / ↓」切換上一頁/下一頁；亦相容一般簡報遙控器（模擬鍵盤方向鍵） |
| 翻頁（手機/平板） | 點擊畫面左右邊緣（左＝上一頁，右＝下一頁） |
| 換頁行為 | 換頁自動回到整頁完整大小，不維持前一頁的縮放/平移狀態 |

### 2. 三大核心工具（工具列點選，互斥切換；亦可用數字鍵 1/2/3 快捷切換）

#### 手套（平移）
拖曳畫面移動位置，用於放大後調整想看的區域。

#### 虛線框（縮放）
取代傳統滾輪/雙指縮放，整合兩種操作：
- **中心點拖曳縮放**：畫面顯示範圍的中心點上下拖曳＝放大/縮小（中心點會跟隨平移後的畫面移動，不是固定螢幕正中心）
- **斜角框選縮放**：畫面任意處斜角拖曳畫出虛線矩形框，放開後畫面縮放至與框選範圍一致
- 手機/平板：單指操作，邏輯相同

#### 筆（手繪）
- 點選後滑鼠/手指變成畫筆，碰觸畫面即繪製
- 旁邊提供顏色選項（不選則使用預設色），粗細可調
- 有橡皮擦/清除鍵，另加「最後一筆復原（Undo）」保護，避免手滑誤刪整頁畫記
- 痕跡保留，換頁不清空，僅能透過擦除鍵手動清除

### 3. 互動邏輯

| 情境 | 行為 |
|---|---|
| 未啟用任何工具 | 點擊畫面無反應，只有快捷鍵（翻頁、還原）會動作，避免誤觸 |
| 還原鍵 | 快捷鍵回到整頁完整大小 |
| 全螢幕播放 | 工具列自動隱藏，滑鼠移到畫面邊緣才浮現，避免投影時被觀眾看到 |

### 4. 存檔

| 項目 | 說明 |
|---|---|
| 觸發時機 | 畫面上有手繪痕跡時，存檔或離開會詢問是否存為新檔 |
| 存檔範圍 | 整份簡報（包含所有畫過的頁） |
| 輸出格式 | 軟體自訂專案檔格式，保留手繪內容可再編輯（非固定 PDF） |

### 5. 設定欄

集中管理所有可調整行為：
- 常態顯示功能開關（例如筆工具常駐開啟）
- 快捷鍵自訂/檢視（工具切換、翻頁、還原等鍵位對應）
- 換頁後縮放行為（目前預設「自動回整頁」，未來可考慮開放為可選設定）

### 6. 手機/平板專屬處理

- 沿用桌面版同一套工具邏輯，單指操作
- 主動停用瀏覽器/系統原生的「雙擊畫面縮放」手勢，避免與自訂縮放工具衝突

---

## 四、技術架構

### 平台策略：跨平台（桌面 + 網頁 + 手機），一套程式碼三端部署

```
             ┌─────────────────────────┐
             │   核心引擎（Web 技術）    │
             │  HTML5 Canvas / WebGL    │
             │  縮放・平移・框選・手繪邏輯│
             │  連結座標解析與疊加層     │
             └───────────┬─────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
   ┌────▼────┐      ┌─────▼─────┐     ┌─────▼─────┐
   │ 桌面版    │      │  網頁版    │     │  手機/平板版│
   │ Electron │      │ 直接部署   │     │ PWA /      │
   │ 支援離線  │      │ (瀏覽器)   │     │ Capacitor  │
   └──────────┘      └───────────┘     └───────────┘
```

| 層面 | 建議技術 |
|---|---|
| 渲染引擎 | HTML5 Canvas，處理縮放/平移/框選/手繪繪圖 |
| 前端框架 | React 或 Vue |
| PPTX/PDF 轉檔 | 本地或後端將檔案每頁轉成圖片（可用 LibreOffice headless），同時解析超連結座標另存為疊加層資料 |
| 桌面封裝 | Electron，預設支援離線播放 |
| 手機/平板 | PWA，進階可用 Capacitor 包裝上架 App Store/Google Play |
| 觸控手勢處理 | 使用成熟手勢函式庫（如 Hammer.js），並主動攔截/停用瀏覽器原生雙擊縮放手勢 |
| 專案檔格式 | 自訂格式，儲存原始頁面圖片參照＋手繪筆劃資料（向量化保存，方便日後編輯） |

**技術選型取捨**：一份程式碼維護三平台，開發與除錯成本低；效能略遜原生 App，但對此類縮放/繪圖需求已足夠。

---

## 五、易用性與邊界情況考量

| 情境 | 處理方式 |
|---|---|
| 簡報遙控器（clicker） | 翻頁功能相容（遙控器多模擬鍵盤方向鍵），但框選/手繪/平移仍需觸控或滑鼠操作，需在說明文件中註明 |
| 離線使用 | 桌面版（Electron）預設支援離線播放，避免會場網路不穩定影響簡報 |
| 誤觸保護 | 未啟用工具時點擊無反應；手繪加 Undo 保護 |
| 全螢幕觀眾視角 | 工具列自動隱藏機制，避免投影畫面出現操作介面 |

---

## 六、開發階段規劃

### Phase 1 — MVP（桌面版優先）
- [ ] 上傳簡報（PPTX/PDF/圖片）並轉為靜態頁面
- [ ] 上下鍵翻頁、還原鍵回整頁
- [ ] 手套（平移）、虛線框（縮放：中心點拖曳＋框選）、筆（手繪）三大工具
- [ ] 手繪 Undo 保護、擦除鍵
- [ ] 存新檔（專案檔格式）
- [ ] 工具切換快捷鍵（數字鍵 1/2/3）
- [ ] 全螢幕工具列自動隱藏
- [ ] Electron 桌面版（Windows），支援離線播放

### Phase 2 — 跨平台擴展
- [ ] 網頁版部署
- [ ] 手機/平板 PWA 版本，觸控手勢優化、邊緣點擊翻頁
- [ ] 停用手機原生雙擊縮放手勢
- [ ] 超連結解析與可點擊疊加層功能

### Phase 3 — 進階功能與優化
- [ ] 快捷鍵自訂設定介面完整化
- [ ] 效能優化（大檔案、高解析度簡報載入速度）
- [ ] 簡報器相容性測試與說明文件

### 明確不做（已討論並排除）
- PPTX 原始動畫效果保留
- 雙螢幕模式（已用「工具列自動隱藏」部分緩解需求，正式雙螢幕先跳過）
- 視角導覽歷史（上一步/下一步瀏覽縮放紀錄）
- 傳統滾輪/雙指縮放（已整合進虛線框工具）

---

## 七、待確認/後續事項清單

1. 雙螢幕模式（講者看備忘稿、投影只顯示簡報）是否要在未來版本中加入？
2. 換頁後縮放行為（目前固定「自動回整頁」）是否要開放成使用者可自訂選項？

---

## 八、下一步建議

1. 確認上述兩項待確認事項的優先順序
2. 畫出詳細 UI 介面草圖（工具列樣式、顏色選單位置、設定欄畫面）
3. 技術原型驗證：先做出 Canvas 縮放/平移/框選核心邏輯的最小可行原型，驗證中心點縮放與框選縮放的操作手感
